<div class="mw-100">
  <h1 class="text-center bg-black p-5 text-white">Cool articles</h1>
    <div class="m-5">
        <div class = "d-flex flex-row-reverse mb-5">
        <%= link_to 'create new', new_article_path, class: 'bg-warning text-dark text-strong p-3 fw-bold' %>
        </div>
          <div class="d-flex mb-3">
            <% @articles.each do |article| %>
                <div class="container mt-1">
                <div class="card p-4 m-1 bg-black">
                  <h2> <%= link_to article.title, article_path(article), class: 'text-white' %>  </h2>
                  <p class= text-white> <%= article.content %>  </p>
                </div>
              </div>
            <% end %>
          </div>
      </div>
<div>
