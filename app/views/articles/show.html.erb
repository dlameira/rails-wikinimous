<div>
  <h1 class="text-center bg-black p-5 text-white"><%= link_to 'Cool Articles', articles_path  %></h1>

  <div class="container">
  <h2><%= @article.title%></h2>
  <p><%= @article.content%>></p>
  </div>
  <%= link_to 'Index', articles_path %>
  <%= link_to 'Edit', edit_article_path(@article) %>
  <%= link_to 'destroy', article_path(@article), data: {turbo_method: 'delete', turbo_confirm: 'sure'} %>
</div>
